from setuptools import setup, find_packages

setup(
    name="Multimodal_RAG", 
    author="Saikumar Talla", 
    version="0.1", 
    packages=find_packages(), # directories contains __init__.py file are considered insider document_portal package. 
)